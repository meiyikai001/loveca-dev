# 卡牌数据管理系统 - 需求文档

> 版本: 1.1.0
> 创建日期: 2026-03-12
> 最后更新: 2026-08-14
> 文档类型: 需求文档
> 适用范围: 卡牌数据管理后台、同步脚本和发布状态流转
> 当前状态: 卡牌数据管理业务需求；当前实现边界见 [设计文档](./design.md)

## 1. 背景

LoveLive! Official Card Game 拥有数百张卡牌，每张卡牌包含编号、类型、名称、效果文本、心图标、费用等大量结构化数据。这些数据是游戏引擎运行和用户组卡的基础。

项目初期，卡牌数据以本地 JSON 文件形式管理，存在以下问题：

- **数据更新困难**：修改一张卡牌需要手动编辑 JSON，容易引入格式错误
- **无权限控制**：任何接触代码仓库的人都可以修改卡牌数据
- **无版本状态**：新卡牌一旦提交就对所有用户可见，缺少审核发布流程
- **缺少可视化编辑**：管理员需要直接操作 JSON 文件，没有友好的编辑界面

## 2. 目标

建立一套基于数据库的卡牌数据管理系统，满足：

1. 管理员能通过网页界面便捷地浏览、创建、编辑和删除卡牌数据
2. 卡牌数据具备 DRAFT（草稿）和 PUBLISHED（已上线）两种状态，新数据默认不对普通用户可见
3. 普通用户和游戏引擎只能访问已上线的卡牌
4. 前端应用无需每次都请求服务器，有合理的缓存机制减轻负载

## 3. 用户角色

### 管理员

- 可查看所有卡牌（包括 DRAFT 和 PUBLISHED 状态）
- 可创建、编辑、删除卡牌
- 可控制卡牌上线/下线状态
- 可导出和导入卡牌数据，用于备份、迁移和批量维护

### 普通用户

- 只能查看 PUBLISHED 状态的卡牌
- 无法修改任何卡牌数据
- 通过游戏界面和组卡界面间接使用卡牌数据

## 4. 功能需求

### 4.1 卡牌浏览与筛选

管理员需要在大量卡牌中快速定位目标卡牌。系统应提供：

- **类型过滤**：按成员卡、Live 卡、能量卡筛选
- **状态过滤**：按 DRAFT、PUBLISHED 筛选，便于集中处理待审核的草稿卡牌
- **关键词搜索**：按卡牌名称或编号搜索
- **分页浏览**：筛选与搜索应作用于完整卡池，并以分页结果返回，避免卡量增长后由浏览器全量加载和筛选

### 4.2 卡牌创建与编辑

三种卡牌类型拥有不同的字段结构：

- **成员卡（MEMBER）**：费用、应援棒值、心图标（颜色+数量的数组）、应援棒心效果
- **Live 卡（LIVE）**：基础分数、心需求（颜色+数量的数组）、应援棒心效果
- **能量卡（ENERGY）**：仅基础字段（编号、名称、效果文本）

所有卡牌共享的字段：编号、名称、真实团体名、小组名、效果文本、图片

编辑界面需要根据卡牌类型动态展示对应字段，隐藏不相关的字段。外部数据源字段名和项目内部字段名的差异应在同步转换层处理，不应暴露给普通编辑流程。

### 4.3 双模式编辑

为满足不同场景下的编辑效率，提供两种编辑模式：

1. **表单模式**：每个字段有独立的输入控件，心图标等复杂数据有专用的可视化编辑器。适合逐字段核对和修改
2. **YAML 模式**：将卡牌数据序列化为 YAML 文本，管理员可直接编辑文本。适合一次性修改多个字段或从其他来源粘贴数据

两种模式之间可自由切换，切换时自动同步数据。YAML 解析失败时应阻止切换并提示错误，防止数据丢失。

### 4.4 DRAFT/PUBLISHED 上线机制

卡牌的生命周期分为两个阶段：

1. **DRAFT（草稿）**：新建卡牌默认进入此状态，仅管理员可见，普通用户和游戏引擎不可访问。管理员可在此阶段补充和校验数据
2. **PUBLISHED（已上线）**：管理员确认数据无误后将卡牌上线。所有用户可见，可用于游戏和组卡

管理员可随时将卡牌在两种状态之间切换（上线/下线），便于临时撤回有问题的数据。

卡牌同步脚本的具体写入策略由卡牌同步设计文档维护。需求层只约束管理后台中的发布状态语义。

### 4.5 AI 辅助效果文本提取

部分卡牌的效果文本需要从卡牌图片中人工转录，工作量大且易出错。系统应提供 AI 辅助功能：

- 对已上传图片的卡牌，管理员可一键调用 AI 识别图片上的日文效果文本
- AI 将识别结果转换为项目约定的中文标记格式
- 提取结果回填到效果文本字段，管理员可在此基础上人工校对
- 浏览器只提交卡牌编号；服务端从权威记录读取卡图并调用部署允许的 OpenAI-compatible 上游
- 管理员可在独立配置页测试和保存 Base URL、Model ID、启用状态与 API Key；既有 Key 不得回显

此功能作为辅助手段，最终结果仍需管理员确认；提取不会自动保存或发布卡牌。

### 4.6 图片管理

每张卡牌可关联一张图片。系统应提供：

- 在编辑界面中直接上传图片
- 上传过程中展示进度信息
- 上传完成后自动关联到当前卡牌
- 普通换图改变 `image_filename` 时，服务端必须清除只属于 CloudBase 版本化对象的内部标记；前端或外部导入不能伪造这组标记

### 4.7 数据导出与导入

- **导出**：将全部卡牌数据导出为结构化文件，用于业务备份、迁移或离线分析；导出内容遵循 API 读取视图，可能包含同基础编号补全后的 `bladeHearts` 派生值，原始数据库备份应使用数据库备份工具
- **导入**：批量导入卡牌数据，返回成功/失败统计，便于从外部数据源迁移

### 4.8 游戏数据加载

普通用户使用的游戏界面和组卡界面需要加载卡牌数据：

- 只加载 PUBLISHED 状态的卡牌
- 加载后的数据应可供游戏、组卡和详情展示按编号查询
- 提供稳定的图片路径解析能力

## 5. 数据需求

### 5.1 卡牌基础信息

每张卡牌需要存储以下信息：

| 信息                     | 说明                                                     | 适用类型     |
| ------------------------ | -------------------------------------------------------- | ------------ |
| 编号（card_code）        | 全局唯一标识，如 PL-sd1-001                              | 全部         |
| 类型（card_type）        | MEMBER / LIVE / ENERGY                                   | 全部         |
| 日文名称（name_jp）      | 日文卡牌名称                                             | 全部（可选） |
| 中文名称（name_cn）      | 中文卡牌名称；至少与日文名称二选一填写                   | 全部（可选） |
| 作品名（work_names）     | 所属作品全名数组                                         | 全部（可选） |
| 真实团体（group_names）  | 所属真实团体短名数组                                     | 全部（可选） |
| 小组名（unit_name）      | 所属小组                                                 | 全部（可选） |
| 日文效果（card_text_jp） | 日文卡牌能力描述                                         | 全部（可选） |
| 中文效果（card_text_cn） | 中文卡牌能力描述                                         | 全部（可选） |
| 图片（image_filename）   | 关联图片文件                                             | 全部（可选） |
| 稀有度（rare）           | R, R+, P, AR, SEC 等；可由同步管线写入，也可由管理员编辑 | 全部（可选） |
| 收录商品（product）      | 来自哪个卡包；可由同步管线写入，也可由管理员编辑         | 全部（可选） |
| 状态（status）           | DRAFT 或 PUBLISHED                                       | 全部         |

### 5.2 类型特有字段

| 信息                         | 说明                         | 适用类型     |
| ---------------------------- | ---------------------------- | ------------ |
| 费用（cost）                 | 出场所需的能量数             | MEMBER       |
| 应援棒值（blade）            | 应援棒数值                   | MEMBER       |
| 心图标（hearts）             | 颜色与数量的数组             | MEMBER       |
| 应援棒心效果（blade_hearts） | 效果类型与可选颜色的数组     | MEMBER, LIVE |
| 基础分数（score）            | Live 成功时获得的分数        | LIVE         |
| 心需求（requirements）       | 完成 Live 所需心的颜色与数量 | LIVE         |

### 5.3 心颜色

系统支持 8 种 Heart 枚举值：PINK、RED、YELLOW、GREEN、BLUE、PURPLE、GRAY、RAINBOW。`GRAY` 是无色/灰色 Heart，只计入 LIVE 判定总数；`RAINBOW` 是 All/万能色，可替代任意指定颜色。管理表单、YAML 与发布数据必须保留两者区别。

## 6. 安全需求

- 卡牌数据的写操作（创建、修改、删除）仅限管理员
- 读操作按状态分级：普通用户只能读 PUBLISHED，管理员可读全部
- 权限控制必须由服务端实现，不能仅依赖前端检查
- AI 服务的 API Key 不得暴露在前端代码、公开配置、日志或错误响应中；当前实现只允许服务端解密并调用上游

## 7. 缓存需求

卡牌数据在短时间内不会频繁变化，但读取频率较高（游戏界面每次加载都需要）。因此：

- 前端应缓存卡牌数据，避免每次操作都请求服务器
- 缓存应有合理的过期时间，确保数据最终一致
- 管理员操作（创建/编辑/删除/上下线）后应及时更新当前列表或使相关缓存失效，保证管理界面数据实时性
- 状态过滤查询不应污染全量缓存，避免 DRAFT 数据泄漏到普通用户视图

## 8. 非功能需求

- **性能**：管理列表应采用服务端筛选和分页，只传输当前页展示所需的摘要字段；卡牌详情和缩略图按需加载
- **可用性**：编辑界面应即时反馈操作结果（成功/失败提示）；图片上传应展示进度
- **数据安全**：编辑模式切换时数据不丢失；YAML 解析失败时阻止切换而非丢弃数据
- **兼容性**：新增的数据库字段应使用可空或默认值，不影响已有数据

## 9. 相关文档

- [设计文档](./design.md)
- [卡牌数据同步管线 - 需求文档](../card-data-sync/requirements.md)
