# 轮换主题牌桌策划与设计

> 文档类型：设计文档
> 适用范围：轮换主题牌桌的产品边界、玩家流程、分配模型、运营约束与第一版实现
> 当前状态：权威设计；第一版开发闭环已落地，尚未开放生产活动
> 产品定位：复用排位赛季时间框架、记录个人胜负但不计分、由服务端提供预组的特殊主题赛季

## 1. 决策与目标

建议将玩家提出的“大乱斗”实现为**轮换主题牌桌**。玩家侧和管理员侧把每一期视为特殊的“主题赛季”，复用排位赛季的名称、周期、开放时段、概览与生命周期心智；规则层仍保持独立 `THEME` 队列。每期记录玩家本人的胜负局数，但不写入 rating、段位或排行榜。

目标是：让所有卡牌已开放的环境中，玩家能以低学习和构筑成本体验不同体系、降低连续撞见相似引擎的疲劳，并验证“预组主题对局”是否提高休闲对局的趣味与复玩。

它不承担以下职责：

- 不修复某一体系过强、卡效错误或禁限用问题；
- 不改变排位的自组合法卡组、单一候场池与计分公平性；
- 不按玩家自组卡组强度配对，也不惩罚玩家使用热门卡组；
- 不建设第三套规则桌面、对局记录或匹配状态机。

## 2. 模式边界

| 项目     | 轮换主题牌桌                                       | 休闲公共牌桌     | 赛季排位         |
| -------- | -------------------------------------------------- | ---------------- | ---------------- |
| 卡组     | 服务端冻结的预组                                   | 玩家自带合法卡组 | 玩家自带合法卡组 |
| 开放方式 | 固定短时段，避开排位核心窗口                       | 常规开放         | 固定赛季开放窗口 |
| 结果     | 记录本人胜负，不计分                               | 不计分           | 计入赛季 rating  |
| 配对原则 | 独立主题赛季池；先 FIFO 匹配，再服务端抽选组 | 单一 FIFO        | 单一 FIFO        |
| 目标     | 尝试、发现、对局多样性                             | 自由对战         | 公平竞技         |

首期建议只在每周固定的 1～2 个短窗口运行，并与排位核心时段错开；不要在验证前全天常驻，以免进一步拆分有限候场人数。

## 3. 玩家流程

1. 玩家在活动页看到本期主题、开放时间、全部预组名称、完整卡表、真实玩法标签与本期 `X 选 1` 规则；后台难度枚举不作为玩家标签展示。
2. 玩家点击加入后先进入独立主题 FIFO 队列。对手就位并形成预留后，服务端才为双方生成本次匹配的候选；`X = 1` 时不显示选择步骤，`X > 1` 时玩家在独立选组弹层中选择 1 副并确认参加。玩家不上传、不修改个人卡组。
3. 候选由预留 ID、当期 `X` 与启用对阵图确定性生成，刷新可重建同一结果，不建立 offer、候选历史或曝光审计表。双方候选的任意交叉选择都必须是当前启用对阵；确认时将最终选择锁入现有票据卡组快照。
4. 双方确认后，服务端在创建房间前的同一权威事务中冻结选择结果、组合版本与锁定卡组；只有分配记录完整成功后才创建现有正式联机房间。猜拳、隐藏信息、`RULES` / `FREE` 协商、认输、历史记录与普通正式对局一致。
5. 对局结束后按本期主题身份更新本人的胜、负、平和完成局数；主题房间不提供原房再战，玩家返回主题入口重新候场并取得新的卡组分配；不写入排位、日常任务门槛或高价值奖励进度。若本期已配置活动纪念徽章，完成局数达到 3 场时可获得该低价值纪念。

主题战绩直接聚合不可变分配记录关联的服务端权威终局：只接受 `COMPLETED` / `SURRENDERED`，中断、损坏、开局失败或没有正式终局的对局不计入。胜率以本期完成局数为分母，平局计入完成局数但不计入胜负。每次重新对战都必须重新候场并形成新的分配记录，不复用公共牌桌的原房再战。首版不为此建立独立积分投影、结算流水、段位或公开榜单，也不向管理员增加战绩开关。

活动纪念徽章复用完全相同的完成局数口径：每期最多配置一枚，首次上传时补发已达到 3 场的玩家，后续在权威终局封存事务中幂等授予。图片替换只改变本期徽章的当前展示资源，不改变资格、不重复授予，也不把徽章升级为积分或高价值奖励。

候选选择不能绕过实测组合矩阵。若两名玩家选择的卡组组合已被管理员停用，两张票据不会互相配对；已经形成预留后再发生配置变化时，确认冻结必须再次失败关闭校验。若玩家在分配后、开局前主动离开，按公共牌桌的开局前离场口径处理；无过错对手恢复候场时保留已选卡组和原 FIFO 时间，不要求再次选择。

## 4. 预组池与配对矩阵

### 4.1 预组不是“主流卡组列表”

每一期推荐 6～8 副预组。选池应同时满足：

- 玩法差异清楚，进入前可由一句玩法标签说明；
- 卡效自动化与规则流程已可接受，不把玩家当测试人员；
- 预组在当前活动规则环境下有可玩的展开与反制空间；
- 有少量高认知度主流构筑作为入口，同时有当前使用率较低的体系；
- 每副均有可公开的完整成员、LIVE、能量清单与版本号。

当前数据支持把 Cerise Bouquet、Edel Note、DOLLCHESTRA、5yncri5e! 视为“玩家熟悉的候选锚点”，而非自动全部入池；A・ZU・NA、QU4RTZ、AZALEA、CatChu!、Mira-Cra Park! 等可作为差异化候选。最终入池必须以本期完整牌表和实测结果为准，不能只按线上份额或社区热度决定。

### 4.2 每一期必须冻结的对象

| 对象                  | 必填内容                                                                                            | 目的                               |
| --------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------- |
| `themeTableVersion`   | 主题名、规则环境、全局卡牌目录版本/哈希、开始/结束时间、公告文案、分配算法版本、候选数 `X`          | 区分不同活动、规则与算法           |
| `prebuiltDeckVersion` | 预组 ID、显示名、完整成员/LIVE/能量表、规范内容哈希、玩法标签、难度、来源、规则完整性检查与审阅记录 | 可复盘每局实际使用的卡表           |
| `matchupPairVersion`  | 两个预组座位 ID（允许相同）、权重、测试记录、启用状态                                               | 防止独立随机产生未测组合           |
| 本期启用对阵基线      | 启用的 `matchupPairVersion` 集合与候选数 `X`                                                        | 候选可由预留 ID、`X` 与确认时启用组合确定性重建 |
| 分配记录              | 对局/预留 ID、主题/预组/组合版本、双方预组 ID、算法版本、可审计熵结果或等价承诺                     | 回放、投诉审查与活动分析           |

预组由服务端所有，不能引用某位玩家的云端卡组；活动启用后不可原地修改。需要修正时创建新版本、停止旧版本继续发牌，并保留历史对局关联。随机种子若用于审计，必须连同算法版本、当时启用对阵基线和服务端熵来源一起保存；单独保存一个可由运营者任意指定的种子，不构成可审计的公平性证明。

### 4.3 卡组池与内部组合的建立方法

1. 先对候选预组做规则完整性检查，并与本期池内已有卡组完成内部试打；没有完成这一步的卡组不得加入卡组池。
2. 管理员可从合法云端卡组加入或直接导入 YAML；服务端按活动冻结的卡牌目录与当前构筑规则校验后冻结快照。未结束赛季均可新增、编辑或移出卡组；编辑创建新的预组版本并停止旧版本继续分配，移出则退休预组并停用所有引用它的当前组合。
3. 每副卡组自动生成同卡组内战，并与池内所有既有卡组生成等权、无方向的内部组合；普通编排不再要求管理员逐组填写权重和试打摘要。
4. 上线后仍按内部组合监控。出现异常时从折叠的“组合异常处理”中立即停用该组合，主题赛季可用剩余组合继续运行。

组合权重固定为等权 1，仅作为审计字段保留，不参与候选生成或分配；不得对某个玩家、昵称、个人自组卡组或历史胜率做定向候选生成。

### 4.4 曝光观察与首期矩阵规模

`X 选 1` 下曝光由玩家自主选择决定，不存在冻结的预期曝光目标，也没有按权重的抽签。运营指标只聚合每副预组的实际选用次数与占比，可按“分配 / 开局 / 完成”三个分母区分口径；候选较少时候选生成受启用组合图约束，曝光集中应解释为玩家选择现象，而不是抽签偏差。

由于卡组池会自动生成内战和不同预组间的完整组合，首期若采用 4～5 副核心预组，对应 10～15 个组合，应保证上线前能够完成每个组合的真实试打。也可以只使用一副预组举办明确的镜像主题。运行期扩充或编辑卡组池必须形成新的预组与组合版本，只影响尚未分配的新对局；运营界面应立即反映当前卡组池，不改写历史分配。

### 4.5 队列与原子分配契约

主题牌桌复用公共牌桌的候场、确认、租约、房间引导和玩家占用机制，但必须拥有独立的 `THEME` 队列上下文与 `themeTableVersion` 环境身份。主题票据入队时仅作为未选组占位；预留形成后，玩家最终选择的预组快照直接写入该票据，不引用临时候选。

形成主题对局时，服务端必须先按双方已选卡组筛选当前启用组合，再在单一事务/可恢复的幂等边界内写入不可变分配记录、关联票据锁组，并取得创建房间所需的租约。任何一步失败都不得产生“已匹配但没有可审计卡组”的房间；失效配置、组合池为空或预组校验失败时，应释放预留并按既有无过错恢复口径处理双方。

## 5. 运营规则与界面文案

活动页必须在候场前明确：

- 本活动不计入排位，且使用平台提供的预组；
- 本活动记录本人当期胜负，但不产生积分、段位或公开排名；
- 本期预组与完整卡表的可见入口；
- 本期是几选一、候选由服务端固定，以及 `X = 1` 时会直接候场；
- 当前开放时间、关闭后的下一次时间，以及不开放时可去休闲公共牌桌或排位；
- 若某预组含更复杂的规则或未完全自动化的交互，要在玩法标签中事先说明。

预组池采用“卡组选择栏 + 当前卡组卡册”的渐进展示：首副预组默认可见，切换卡组不折叠整个页面；完整卡表按成员、LIVE、能量分区，每个基础展示项只出现一次并以 `×N` 标注数量，同时显示卡名。首页封面、开场分配动画和卡册都必须通过运行时卡牌目录的权威 `imageFilename` 解析图片，只有目录缺失对应卡牌时才按卡号降级。点击卡图复用全站只读卡牌详情抽屉；移动端选择栏横向滚动、卡册固定四列，详情抽屉必须支持显式关闭、`Escape`、焦点约束与关闭后返回触发卡图。卡组编号和卡表哈希仅作为补充审计信息，不得成为玩家理解预组的主要载体。

不提供重抽、禁用卡组或投票 ban。候选数由赛季管理员配置为正整数，实际候选数不超过当前可用卡组数；为保持直接体验，`X = 1` 不显示中间选择页面。

## 6. 需要复用与新增的系统边界

复用既有公共牌桌候场、双方确认、正式联机房间、权威对局、卡组锁定快照、观战、认输、重连和历史记录。主题牌桌的新增责任仅包括：

- 主题赛季窗口与版本读取，前端复用排位管理的页签、开放时段控件和列表结构；
- 服务端预组注册表和配对矩阵；
- 匹配后按活动配置生成稳定临时候选，并在确认时校验和锁定玩家最终选择；
- 将活动、预组和配对版本随对局快照保存；
- 按主题版本与权威终局聚合玩家本人当期战绩；
- 按需配置本期活动纪念徽章，并按相同的权威完成局数口径幂等授予；
- 独立的活动指标聚合与管理员停用入口。

服务端负责本次候选生成与最终选择校验；前端只展示候选并提交卡组 ID。管理员“停用组合”会让未确认的候选在下次读取时重建；已冻结的分配与已开始对局保持原版本。

## 7. 指标、停开条件与首期判定

### 7.1 每个活动窗口采集

- 候场人数、等待时间、确认率、房间创建成功率、开局率、完成率、认输/断线/`FREE` 使用；
- 各预组曝光次数、获胜次数、先后手、镜像率与每个预组组合的样本量；
- 预组组合层面的完成率、异常处理率和胜率，而非只看单副预组总胜率；
- 每副预组的实际选用次数与占比，按“分配 / 开局 / 完成”三个分母分别显示；
- 同窗口休闲公共牌桌与排位的等待、完成与对局量变化；
- 参加者在 7 / 14 天内的再次对局、活动前后本人对局量变化，以及自愿反馈中的“理解玩法”“想再玩”“不公平”“等待过长”。

### 7.2 首期的继续、调整与停止

首期只验证可玩性和增量参与，不以高参与数或单次好评直接常驻化。

| 判断                                                                                               | 建议动作                                                                 |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 分配、锁定或房间引导出现不可审计结果；无过错对手无法得到既定恢复结果                               | 立即暂停新入队，保留证据并先修复公共牌桌/分配链路                        |
| 某组合达到预先冻结的最小完成样本后，胜率区间越过容忍范围、完成率低于下限，或产生超过上限的人工处理 | 立即从配对矩阵停用，复测预组；不改成按玩家定向候选                       |
| 单副预组实际选用占比持续明显偏离池内均值                                                           | 复评该预组强度、玩法标签与组合矩阵；不得通过面向特定玩家的定向候选修正 |
| 活动明显拉低同窗口休闲/排位的完成率，或使等待时间不可接受                                          | 缩短窗口、移到其他时段或暂停，而非再开一个队列                           |
| 预组曝光符合目标、完成率可接受、玩家复玩和人均对局量提升，且未只是搬运原有对局                     | 保留并以新池轮换进行第二期验证                                           |
| 平台数据发现某体系跨自组模式长期压制                                                               | 单独启动竞技环境、卡效正确性与禁限用审查；主题牌桌不掩盖该问题           |

在首期开始前，运营应填写具体的最小样本、可接受等待时间、组合胜率容忍范围、完成/异常处理下限和基线窗口，并冻结在活动记录中，避免事后修改成功定义。基线至少固定同星期、相邻时段的对照窗口、观察回溯长度、测试/管理员账号排除规则和“净新增”的计算方式；主题桌的局数不能单独当作增量参与。

## 8. 上线顺序

1. 先收束公共牌桌的开局前离场无过错恢复、进行中房间恢复和指标聚合；主题桌不以非计分为由绕过这些前提。
2. 建立可审计的主轴/核心组件标签与快照质量原因码，保证活动外的环境基线可信。
3. 选出 6～8 个候选预组，冻结完整清单并做规则与少量对局组合试打。
4. 建立独立主题队列上下文、活动版本、服务端预组、配对矩阵、原子分配/锁定记录、曝光校验和管理员停用能力。
5. 用短时段进行首期非计分试验；窗口避开排位核心时段。
6. 依据冻结指标决定保留、调整、暂停或进入第二期；不从首期直接推广为全天常驻模式。

## 9. 关联文档

- [平台产品发展路线草案](./PRODUCT_DEVELOPMENT_ROADMAP_DRAFT.md)
- [赛季排位首版当前状态与上线边界](./RANKED_INITIAL_IMPLEMENTATION.md)
- [系统设计](../system-design.md)

## 10. 2026-08-02 第一版落地评估与实现状态

总体判断：产品边界与服务端预组原则合理，可以进入实现；首期应继续保持短窗口、非计分和服务端预组，不应加入独立抽牌或高价值奖励。当前方案最重要的正确点是把组合、分配与锁组的冻结放在服务端完成，同时公开完整卡表、限制候选组合，并把活动身份与卡组快照留在服务端。第一版“随机分配、不提供选组”的结论已被第 11 节的 `X 选 1` 调整推翻。

第一版已经落下以下基础闭环：

- 新增独立 `THEME` 队列上下文和 `THEME_QUEUE` 玩家占用，不与休闲或排位候场混用；
- 新增活动版本、服务端预组版本、组合版本和不可变分配记录；
- 主题票据入队时不上传个人卡组；预留形成后选定服务端预组快照，双方确认后在同一数据库事务中校验已启用组合并冻结分配记录；
- 只有分配冻结成功后才进入现有房间创建租约；房间继续复用正式联机、权威规则、卡组快照、观战、认输和历史记录；
- 玩家页可查看本期说明、开放状态、全部预组、真实玩法标签和完整成员/LIVE/能量卡表；后台难度枚举不对玩家展示。预组池已使用卡组选择栏与卡图卡册，单卡显示名称和数量并可打开复用的只读详情抽屉。玩家可候场、确认、跨页面保活和进入已创建房间；
- 玩家页按主题版本展示本人的完成局数、胜、负、平与胜率；数据直接来自分配记录关联的权威终局，不创建积分、段位或排行榜；
- 数据迁移为 `drizzle/0026_add_theme_table_v1.sql`，`drizzle/0036_add_theme_deck_choice.sql` 增加候选卡组数字段；主题分配可通过 `reservationId` 与最终 `matchId` 审计。

第二实施批次继续完成：

- 管理后台已收敛为与排位一致的“概览 / 主题赛季”框架，主流程只有“填写赛季信息 → 维护卡组池 → 开始赛季”；技术环境与评估阈值使用冻结默认值，未结束赛季支持云端加入、YAML 直导、移出和复用共享卡组编辑器修改构筑，卡组加入或替换时自动生成内战及与池内既有当前卡组的等权组合，单组控制只保留为折叠的异常处理；
- 数据库只允许一个 `ACTIVE` 版本。运行期卡组编辑不会原地修改预组，而是退休旧版本并生成新版本；移出卡组会停用其组合，最后一个可分配组合消失时在同一数据库语句中自动暂停新入队。活动环境身份、评估口径、旧预组、旧组合和既有分配仍保持冻结；
- “已创建房间、尚未开局”阶段一方主动离开、未到场或整体开局超时后，责任方票据终止；无过错方在活动仍开放时取得新票据、保留原 FIFO `joined_at`，并以 `requeued_from_ticket_id` 留下审计链；
- 管理工作台已展示入队、分配、开局、完成、无过错回队，以及按预组统计的实际选用次数与占比。首期成功定义随活动冻结，不允许事后改口径。
- 管理员可为任一期活动上传或替换纪念徽章；首次配置会补发已完成 3 场的玩家，后续授予与权威终局封存同事务完成，玩家仍只在个人中心读取本人徽章。

当前实现仍只适合开发/内测环境：尚未建立首期真实预组池和实测配对矩阵；进行中房间跨进程恢复、完整等待/确认/组合胜率与异常率聚合、选用分布告警、主题身份历史详情和 focused 双浏览器验收仍未闭环。环境调研中的主流/特色体系不能直接当作已批准活动卡表；完成真实试打和上述上线前置前，不得发布生产 `ACTIVE` 版本。

## 11. 2026-08-27 候选卡组选择调整

后续玩家反馈推翻了第一版“不提供选组”的产品假设：完全随机到不感兴趣的卡组会直接降低开局意愿。当前权威方案升级为 `THEME_DECK_CHOICE_V2`，由赛季管理员配置 `X`，先匹配对手，再由服务端生成本次匹配的稳定候选。

- `X` 允许为 1；此时仍保留第一版单击加入、直接候场、确认后揭晓卡组的界面节奏，不展示选择区域。
- `X > 1` 时在匹配确认层展示独立候选卡组组件，玩家选定后与“确认参加”合并提交。
- 匹配前的候场状态可退出；对手就位后的选组或确认状态不提供主动退出控件。
- `X > 1` 玩家已在选组界面看过候选和完整卡表，进房后直接进入猜拳，只保留一行本局卡组名称；只有 `X = 1` 才播放一次卡组揭晓。
- 候选不持久化，也不建设候选曝光审计；同一预留 ID 可确定性重建同一候选，阻止刷新重抽。
- 匹配器先按 FIFO 形成预留，候选生成再保证双方任意选择都不会绕过管理员停用的异常对阵。
- 无过错回队保留已选卡组和原候场时间，不让玩家为服务端／对手异常重复选择。

该调整发生在娱乐模式首次生产开放之前，因此不保留旧分配运行时兼容路径，也不把旧开发活动数据视为长期业务事实。
