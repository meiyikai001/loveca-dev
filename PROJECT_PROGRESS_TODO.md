# Loveca 当前进度及待办

更新时间：2026-09-12

> 本文件只保留当前基线、仍有效的缺口和下一步。文中生产状态来自既有部署记录，不能由本地代码核验；涉及待部署迁移或上线验收时，应重新核对目标环境。已经完成的逐窗口施工记录不再重复保存；需要追溯时使用 Git 历史。卡效完成状态以主登记册为准，发布与迁移历史以对应 runbook 和 migration notes 为准。

## 当前基线

- AI LIVE 设置已增加组合成功率基线，以及 Codex/API 共用的可选只读批量条件查询（每窗口一批、最多 8 组完整盖牌＋补心配色＋追加声援）。基线不增加调用；查询后的最终回答正常计费、保留原预算/超时/失效保护与归档。纯数值计算覆盖无放回、补抽/已知顶牌/一次刷新，不重建历史牌群或验证未来卡效假设；真实模型策略与整体延迟仍待用户试玩验证。见 [计算口径与试玩方式](docs/ai-battle/live-probability.md)。
- 本地 Codex 修复 T6 正常长局触及公开历史 128 KiB 上限：扩至 256 KiB、整批校验后写入、分别展示历史／单次请求容量及具体字节。保持 512 KiB 请求保护、原 token／调用预算与完整历史，不改策略。停止局 56 个相关窗口离线重放通过，含第 290 窗口；更长真实局仍待用户试玩，见[容量说明](docs/ai-battle/local-codex.md#用量隔离与停止行为)。

### 规则与对局

- `GameSession` / `GameService` 继续作为权威状态和命令处理边界；玩家输入统一通过语义化 `GameCommand` 和中央命令政策校验。
- 新对局默认使用权威 `ManualOperationMode=RULES`。本地调试、对墙打和远程调试可在安全时点直接切换；正式联机开启 `FREE` 需要双方协商，观战与回放始终只读。
- 普通登场、换手、费用支付、LIVE 设置、判定、成功 LIVE 选择、卡效 pending/activeEffect 和主要阶段流已经进入共享规则链路；LIVE 设置时点通过独立语义支持盖牌与撤回本轮里侧盖牌，不放宽规则模式的通用区域移动。
- 声援规则的公共结算边界已统一：普通、追加、重做及 `FREE` 手动单张声援均在本批公开后立即且仅一次结算 DRAW BLADE HEART，然后才进入适用的 `ON_CHEER` 检查时点；玩家接受最终判定结果时不再抽牌。重做声援可替换当前 Heart/Score 贡献，不撤销原批已完成的抽牌。
- 本次声援的 BLADE HEART 条件已有公共有效值底座：`selectCurrentLiveRevealedCheerCardsWithEffectiveBladeHearts` 按 event-inclusive 公开事实返回逐卡有效判心，并应用 Dazzling Game 等持续改色；颜色并集、不同卡覆盖与有效 ALL 等上层条件必须从该结果分流，不能回退到印刷 `card.data.bladeHearts`。固定卡文颜色集合继续显式列举，不自动吸收 GRAY/ORANGE。
- 全卡池完整自动裁判尚未完成；未自动化能力不能依靠 UI 或具体卡牌特例绕过规则边界。

### 卡效框架

- 具体卡效定义集中在 `src/application/card-effects/definitions/index.ts`；单卡与 shared family 分别位于 `workflows/cards/` 和 `workflows/shared/`。
- `card-effect-runner.ts` 的完整卡效 fallback 已清空，只保留调度、生命周期、registry 和尚未迁出的 matcher/relay/trigger 条件胶水。
- 当前已登记的 implemented definition 和基础编号均可在 `docs/card-effect-reuse-audit/existing_module_map.md` 检索；该文件是卡效完成状态的唯一主登记册。
- 新卡效继续优先复用现有费用、检视、区域选择、成员状态、能量、抽弃、声援和 LIVE modifier 底座，不建立大型 resolver DSL。
- HEART modifier 已按卡文显式区分 `SOURCE_MEMBER` / `TARGET_MEMBER` / `PLAYER`：self-selection 仍为 TARGET，真实来源与受益成员分别记录；RULES/FREE 离场、替换、memberBelow、同实例重登场和 authority checkpoint 复水共用严格生命周期边界。

### 联机、观战与回放

- AI 对战管理员调试首版已形成开发基线：平台管理员使用固定构筑与手册，通过共享牌桌对战 qwen3.8-flash（默认）/ qwen3.8-max / glm-5.2 / deepseek-v4.1-flash（策略验证仍以真实模型专题为准，GLM-5.2 已有关闭思考的对战导出复盘，策略仍未通过验收；DeepSeek-V4.1-Flash 已有对战导出复盘，策略仍未通过验收；DeepSeek 按北京忙时公开价估算）；创建页可按局选择思考开关（默认关闭），状态随会话和模型请求配置冻结；同队列采样/条件提交、失败停止、可重试结束和有界决定观察/导出已贯通。LIVE 设置由一次 `CARDS` 多选提交最终盖牌完整集合，服务端在同一权威队列内完成必要撤回、追加放置与确认，不再逐张请求模型。真人与 AI 均可选原始 μ's 预组、绿莲-6弹ver和 Like a Treasure 并进行镜像或交叉对局；另提供 AI 专用的蓝紫构筑，AI 席只豁免 PT 总数上限，其他构筑规则与当前发布卡库校验不变。AI 输入由合法可见视图生成，附己方休息室简表、起动费用/目标数与实际结算摘要；模型自述只留观察材料，不再回灌决策上下文。观察框默认隐藏等待及机械处理记录，可分别按需显示。完整隔离环境与真实模型验收见 `docs/ai-battle/`，已接入北京公开原价的每决定费用提示及整局持久累计；新局上游与 Key 从平台 AI 配置读取并冻结，部署需先执行 0040 计费迁移；生产开放另行处理。
- 本地前端验收已完成：4 次 Luna medium 全部接受，3/3 续轮缓存、一次选择两张 LIVE 并确认、订阅账本与导出一致；短测在 T1 手动结束。仓库外新增可复用本机 API 启动器，显式启用同线程/自动换线程，按局设 60 次/900 万输入/150 万非缓存/5 万输出预算，不改生产默认或 `.env`。前端仍需选择 medium；预算不代表套餐额度。下一步为用户完整试玩反馈与策略/调用效率优化，另有批量命令摘要和结束观察提示两项展示问题。详见 [本机使用说明](docs/ai-battle/local-codex.md#当前本机试玩入口2026-09-16)。
- 修复版自然局已在T6终局：44次Luna medium全部合法执行，4次实际水位换线程、39/39续轮缓存通过；输入5,728,233/非缓存1,084,649、全部报告，余16次。牌顶失效修复覆盖3次顶牌/7窗口的真实审计通过。最终有界导出淘汰72条，但逐次快照覆盖决定1～157；不声称单份导出完整。运行源码未新增，沿用同版62项及服务端类型检查，新增完整局与独立审计。下一步转本地前端验收/使用说明，不再重复短缓存实验；策略理由及调用效率仍待优化。详见 [本地 Codex 接入](docs/ai-battle/local-codex.md)。
- 此前真实规模只读回放6次后，第6窗口MAIN/ACTION误答上一EFFECT/CARDS任务，原校验拒绝并停止；未达自动切换水位、未执行动作。输入287,524/非缓存102,180，当时余额13次。Codex末尾补当前窗口摘要，保留固定schema和原校验，2文件99项及服务端类型检查通过；后续短样本见上条。见 [本地 Codex 接入](docs/ai-battle/local-codex.md)。
- 此前3次Luna medium合成验证通过实际createSuccessor及新线程续轮：上下文13,765→8,313→8,426；续轮输入8,378/缓存7,936，三轮引用与事实正确。总输入30,300/非缓存22,364，当时余额19次。低水位提前切换不等于真实规模自动换线程或自然局验收；间歇性失败根因未明。
- 后续合成诊断曾首轮即TURN_FAILED，未进入换线程，1次用量未知后停止；用户另行确认后，同份prompt/schema单次成功（输入8,246/输出93），当时余额22次。已补CLI错误类别/HTTP状态白名单，2文件98项及服务端类型检查通过；最新切换验证见上条。
- 2026-09-16 换线程自然局尝试16次 Luna medium：前15次合法执行、14续轮命中，已知输入1,585,100/缓存1,363,456/非缓存221,644。T4首次换线程后的第16次调用失败且用量未知，已停止，未自然终局/重试/重开；本轮40次授权余24次但仍遵守失败停止条件。新请求115条历史事件均核对来自本席已送达输入。新增安全会话失败分类，2文件91项与服务端类型检查通过；先做有界换线程合成诊断，暂不默认启用或交付作者。详见 [本地 Codex 接入](docs/ai-battle/local-codex.md)。
- 本地 Codex 新增默认关闭的 THREAD_ROTATION 实验开关，要求 SESSION_REUSE：原80%水位后换新线程，携带已送达本席的公开事件（缺口明确）、固定资料及当前输入，保持计费与账号约束。真实窗口只读回放3次，16～17窗口候选与事实通过，续轮输入67,924/缓存48,896；首个15窗口因审计枚举格式停止，未重试。未执行回放建议或新开自然局，尚未在测试服启用。5文件112项、服务端类型及diff检查通过；授权累计30/40，剩10次。预算仍为显式可选，下一步做实验开关的有界自然局验收。
- 本地 AI 复盘新增默认关闭的按局完整归档：显式本地部署开关与仓库外目录启用能力后，可在建局页勾选；脱敏事件在内存裁剪/淘汰前异步写入JSONL，观察面板可独立下载完整归档。单局256MiB、队列8MiB，写入失败/超限明确标记不完整且不停止对局；旧局不能补录，原有限缓存及生产默认行为保留。详见[观察与归档](docs/ai-battle/runtime-and-observation.md#可选的本地完整归档)。
- 本地 Codex 已整合作者 AI 分支 `ea66bb73`，保留多模型 API、思考开关与批量盖牌。显式 app-server 恢复同线程续接，首轮固定资料、后续作者当前输入，容量保护使用实际 token 与报告窗口的 80% 水位。修复试打 Bot 时间门禁后，新局 16 次 Luna medium 均合法执行，15 个续轮全部命中；输入 1,784,606，缓存 1,554,688，非缓存 229,918。三次 LIVE_SET 分别一次完成 2/1/2 张设置与确认。T4 达到本地上下文水位，第 17 窗口未发送、未自然终局；截至压缩诊断阶段使用23/40次（后续调用与余额见上条）。原生压缩后缓存可恢复，但旧用户局面仍完整保留，不能解决当前增长；新增显式诊断接口，正常对战不自动压缩。离线受限工具结果可被移除，该工具压缩路线暂缓，优先验证低频新线程切换（见上条）；默认 Luna low、环境和测试服未动。见 [本地 Codex 接入](docs/ai-battle/local-codex.md)。
- 正式联机已具备房间号双人房间、云端卡组锁定、双方准备、暗选猜拳、胜者决定先后手、服务端权威命令、轮询同步、请求式撤销/重开、主动认输和短暂断线恢复；认输以 `OPPONENT_SURRENDER` 结束对局并封存为 `SURRENDERED`，适用于普通与公共牌桌房间。
- 房间号观战使用授权玩家视角，支持同会话切换、跨重开等待和最多 10 个普通观战会话；观战不提供命令、上帝视角或对手隐藏信息。
- 正式联机对局已提供按 `matchId` 隔离的轻量局内通信：双方可发送纯文本或当前运营目录中启用的快捷表情，已授权观战者只读；版本化目录、内容寻址 WebP、上传重编码、管理员编辑、公开配置、发送时目录校验与消息资源快照已接通。初始迁移提供六项种子表情，当前默认目录追加“跟你爆了！”、“Oh no!”、“我 LIVE 呢”三项统一手绘贴图用于测试，合计九项。文字聊天保持顶层一键直达，快捷表情入口位于己方身份旁，对手表情会在发送者身份旁短暂展示。文字与表情共用序号、游标补拉、未读、静音、幂等和综合限频，表情另有 2 秒冷却；目录和资源元数据持久化，但单局消息仍只跟随内存运行态，不写入对局状态、历史记录或回放。
- 正式联机与服务端可记录对墙打已经写入历史根记录、参与者、卡组快照、timeline、authority checkpoint、public/private event 和部分 decision record。
- 完整历史回放默认保留最近 10 天；过期已封存对局可在停机维护窗口清理时间线、检查点、事件、决策与卡组明细，根记录、参与者和卡组来源元信息继续保留并标记为 `METADATA_ONLY`。
- 公共牌桌 Beta 一期已落下首个可运行闭环：PostgreSQL 只保留候场票据、配对预留和跨模式玩家占用三张运行表；锁定卡组直接内嵌在票据中，生命周期事件改走结构化应用日志。其余闭环包括 FIFO 原子认领、双方确认、带短租约和有限重试的封闭房间引导、`PUBLIC_TABLE` 对局来源，以及跨页面等待和确认 UI。公共牌桌自动房间使用 6 位易读房间号，房间号观战与普通房间一致默认开启；卡组数据通过 transport serde 无损保存 `Map` 等运行时类型，并覆盖从 JSONB 往返、猜拳开局到首个玩家快照的回归。联机页在对局快照失败时会保留明确的失败/重试界面。后续仍需在完整测试环境持续验证双浏览器真实流程、进行中房间恢复和运营聚合。
- 公共牌桌经过一段时间的实际使用观察后，玩家已经形成在统一入口持续候场的使用习惯，产品需求验证不再阻止下一阶段排位设计。首个赛季排位按独立赛季页面、固定活跃时段、单一 FIFO 排位池、Glicko rating 和复用正式联机房间的方向推进；休闲公共牌桌与房间号继续不计分。
- 轮换主题牌桌第一版开发闭环已落地并在首次生产开放前升级为 `X 选 1`：玩家先进入独立 `THEME` FIFO 队列，匹配成功后服务端再按本次预留稳定生成互相可对阵的候选。赛季管理员在草稿中配置正整数 `X`；`X=1` 不展示选择步骤，确认时直接锁定；`X>1` 通过独立弹层选定 1 副再确认对局。候选不建立 offer 或曝光审计表，可由预留 ID、候选数和启用对阵即时重建；最终选择锁入现有票据卡组快照，双方确认后冻结组合再创建房间。当前仍是开发/内测基线；真实预组试打矩阵、跨进程房间恢复、完整组合指标/告警和双浏览器验收未完成前不得开放生产活动。
- 赛季排位 R1 已完成首轮生产影子验证：2026-07-24 至 29 日的 491 局合格公共牌桌对局覆盖 50 名玩家，未显示明显首后手偏差；5 场资格会让仍高 RD 的短期连胜玩家直接进入榜首，因此当前候选提升为 `GLICKO1_PER_MATCH_SHADOW_V2`，只把定位/排行榜门槛提高到 10 场，Glicko 数学参数不变，V1 继续保留用于复现原报告。`glicko-shadow.ts` 可按 `settledAt + matchId` 稳定回放；临时生产脚本仍只读查询 `match_records` 并匿名生成 JSON/Markdown，V2 已补充排除状态与正确的卡牌数据统计口径。现有 `match_records.cardDataHash` 是本局双方卡组快照哈希，不能作为赛季全局卡牌环境身份。详细判断见 `docs/matchmaking-and-ladder/RANKED_SHADOW_REPORT_ANALYSIS_2026-07-30.md`；`SHADOW` 版本不得写入正式积分。
- 赛季排位首版闭环已落地，名为 V1 的首个赛季已在生产开放并使用 `GLICKO1_PER_MATCH_V3`。正式 V1–V3 继续冻结以确定性重建旧流水；未来新赛季默认 V4，使用初始 `1500 / RD 300`、`ratingScale=800`、最低 `RD 100`、5 场定级和双方定级后的 1800 中心成长池；实时结算与 ledger 重放已收口到同一算法调度入口，正式 V4 在普通赛季配置中将定级与参榜门槛锁定为 5 场，不需要评分结构迁移。`0010_add_ranked_system.sql` 提供排位基线，`0018_add_ranked_season_announcement.sql` 增加可编辑纯文本赛季公告；赛季生命周期/开放窗口、独立排位票据与稳定 FIFO、幂等且可补偿的 `RANKED` 开局绑定、带在线代际保护的断线裁定、权威终局自动结算、追加式 `VOID / REPLACEMENT`、签名更正预览、软重置种子和确定性重建均已实现。排位断线重连期限取本局开局时冻结的平台配置，默认 1 分钟：双方都超时且最后在线时间相差不超过 5 秒时为平台无结果，超过 5 秒时由较早离线方判负；该政策作为不可缺省的固定公告内容显示在赛季页、候场状态和局内房间菜单，管理员填写的赛季说明显示在同一弹窗。软重置默认使用新赛季初始积分/RD，草稿期可由管理员选择并参数化向中心值保留公式；策略和参数随竞技环境冻结，种子积分在玩家首场结算前不展示。进入 `FINALIZING` 前已经形成的配对可继续开局，未开局预留消化完之前不能封存；收口任务先排空可靠结算，不能只改数据库状态终止仍在运行的对局。玩家侧已有精简赛季页、限高滚动卡组选择、跨页面候场/确认、首场积分与变化回显、参榜场次门槛、个人战绩、最近对局和排行榜；管理员侧已有“概览 / 赛季 / 对局处理”页面，概览按赛季展示运行健康、经营统计、玩家场次数与积分分布，并提供默认前三、可搜索及展开的卡组分类胜率与玩家明细，以及默认收起、可居中定位目标并向前后持续加载的全部有效参赛者表；赛季列表可为全部生命周期展开只读配置快照，已结束赛季仍可核对最终赛程、完整积分参数与冻结竞技环境；V1–V3 可配置 1–100 场参榜门槛，正式 V4 普通配置锁定 5 场，并可修改名称、公告和开放时段，空的未开始赛季可经二次确认永久删除；对局处理默认分页读取全部赛季，支持赛季、计分状态和用户筛选，并直接展示双方胜负、加减分、胜方与结果类型。V2→V3 停机迁移工具和 runbook 保留为历史运维与审计资料，V4 不使用该迁移路径。
- 排位单一责任玩家操作超时已进入开发基线：待决卡效、检视、费用、选择及明确单人推进阶段统一由服务端权威查询归责；责任玩家超过本局开局时冻结的平台操作时限仍没有成功游戏命令时判负，当前默认 3 分钟。成功操作会重置完整时限，轮询、拒绝命令和对手操作不会续时。裁定沿用 DB 一次性认领并以 waitKey/generation 二次复核防竞态；双方快照投影责任 seat、窗口开始时间与截止时间，共享桌面最多在最后 1 分钟提示。该规则补充同一对局快照中的断线重连裁定，非排位、观战与历史回放不启用，尚待发布和生产观测。
- 对战操作超时与断线重连期限已收敛为平台级单例配置，默认仍为 180 秒与 60 秒，不在赛季或活动模型重复字段。运营人员可在平台配置页统一调整；每场新对局在开局时冻结当时值，进行中对局不受后续修改影响。当前自动操作超时与断线判负仍只在已启用对应裁定的排位路径执行。
- 赛季卡牌使用率与卡组分类环境已形成开发基线：排位注册事务会将双方锁定主卡组规范化为长期观察事实，不同罕度按基础编号合并，并保留可重建分类所需的稳定构筑指纹。玩家页按赛季提供卡牌使用占比、胜者构成和高排名玩家三个各自独立的 Top 30 TAB；卡组/卡牌各自保存展示内容和基础计权、仅共享 N，卡牌统计不依赖分类发布，而“胜者构成”统一表达胜方使用了什么，不是胜率。分类发布快照冻结分类名称与规则，`UNKNOWN / AMBIGUOUS` 保持独立；管理员概览按当前 `ACTIVE` 发布和严格双方可分析样本，以卡组席位统计每类卡组及各玩家的使用次数、胜负和胜率，镜像局计 2 次使用且一胜一负，并且不受玩家端展示开关影响。全部 `ratedMatchCount > 0` 的赛季参赛者可按真实全局榜位搜索并持续分页读取，逐行显示评分、RD、场次、胜负和最常用分类；旧的单玩家上下各最多 3 名上下文接口继续保留。管理员对局处理页可按需展开双方长期主卡组，并明确标识早期缺失观察的历史对局。卡组分类样板支持从历史排位对局筛选并选择玩家席位导入，支持双方玩家、日期和赛季筛选，并展示当局用户卡组名称；导入使用长期观察，缺少观察的席位不可选，回放清理不影响已有长期观察导入。所属系列／分组支持已有选项与自定义输入；样板库支持 YAML 文件只读预览后确认导入，服务端重新校验主卡组与卡库、按基础编号合并罕度并使用现有 MANUAL 来源保存，重复构筑提示查看已有样板，能量卡不纳入分类。本批管理交互改动无需新增数据库迁移。`0020_add_ranked_deck_observations.sql` 提供长期事实，最终单一 `0032_add_deck_classifier.sql` 提供分类、发布、assignment 与两套独立展示设置；能量卡组与逐张实例不属于长期观察。生产仍需按对应 runbook 执行首次 0032、种子导入与分类发布。
- 玩家徽章已从首届排位专用目录扩展为排位／娱乐模式统一活动配置：赛季管理员可为任一赛季或活动上传、替换徽章，图片存入公开对象存储并按活动规则引用；配置后玩家完成 3 场有效对局自动幂等获得，中途上传会在保存事务内补发已达标玩家，替换图片不重复授予且既有持有者同步显示新图。排位由评分事务授予，娱乐模式由权威对局封存事务授予，资格证据固定为第 3 场；个人中心仍只读取本人徽章。`0035_add_activity_badges.sql` 扩展双活动来源、图片 revision 和幂等／审计字段，首届旧静态图需按停机迁移说明上传对象存储后再部署新运行时。
- 进行中 V3/V4 赛季已支持受限评分参数修订：15 分钟签名 dry-run 绑定 config 哈希、操作人与 ledger revision；应用只允许 `ACTIVE + PAUSED`、对局/票据/预留/占用清空且冻结环境一致，并在可串行化事务中创建唯一新修订身份、追加指令、全量回算投影/单局快照和保存完整新旧 config 审计。参数恢复也经同一预览/回算路径，不覆写 V1–V4 原始正式身份；V4 可调整成长补偿启用状态、基准分、单局总调整上限、高分局回收分配和过渡宽度，关闭时保留其余成长参数；回算预览按显示名称/用户名展示玩家，不再要求管理员识别 UUID。该增量 schema 由 `0017_add_ranked_rating_revisions.sql` 提供。
- 普通历史读取使用 `/api/battle/match-records...`：checkpoint 节点返回对应玩家视角的轻量只读投影，公共事件、该视角私密事件和可见决策只在展开调试详情时按节点上界分页读取；旧 `/api/online/match-records...` 仅保留为已公开协议的临时 alias。快速切换会取消旧 context/node/audit 请求，客户端按 `matchId + viewerSeat + checkpointSeq` 复用同 key 在途请求并保留 12 个节点的页面内 LRU；服务端只对已封存、非进行中且未清理的完整记录缓存已验证玩家投影，命中前仍重新校验权限、记录状态和 checkpoint 身份，缓存按条目数与估算字节双重限制。回放性能探针与相邻节点预取均为显式配置，预取默认关闭；固定真实数据回放已通过，仍需以生产长局采样决定是否启用预取并完成浏览器性能验收。
- 对墙打运行态缺失时可以从最近 authority checkpoint 恢复；正式联机进程重启后恢复进行中对局尚未闭环。

### 前端与数据

- 前端外层 P1/P2 稳定化与阶段 A 观测基线已进入开发基线：版本发现统一收口到 prompt 型 `UpdateCoordinator`，应用先渲染，Service Worker 与 `version.json` 只标记更新可用；进行中本地/远程对局不展示更新入口，只有用户在安全页面确认后才允许单次刷新，不再清空卡图、表情等运行时缓存。云端卡组继续由唯一 `deckStore` 持有，并按登录用户隔离 30 秒会话 freshness、合并同用户并发请求，区分冷加载与保留旧数据的后台刷新；首次卡组读取与 PT `ensureLoaded()` 并行，挂载、手动刷新和写后确认已有明确边界。统一 User Timing 已覆盖导航、surface、首批数据与后台刷新，标准 E2E 会记录 API/document 请求和完整本地对局 trace，可重复脚本固定统计初始 chunk、route chunk 与 PWA precache；参考构建初始 JS 为 4,010,638 bytes / 895,981 bytes gzip。本地 production 双 build PWA 验证已确认新版本发现不会自动导航、确认后只刷新一次且运行时缓存保留；正式发布前仍需用真实账号完成大厅/准备页/卡组页往返请求计数，以及本地对局、服务端对墙打、正式联机、普通网页、PWA、无痕与移动端矩阵验收。
- 平台维护状态已从公告生命周期收敛为 `NORMAL / RESTRICTING_NEW_GAMES / MAINTENANCE` 三态；独立持久快照、API/快照维护优先门禁、维护与意外故障分流页、管理员恢复入口、数据库就绪检查、结构化状态日志和离线快照工具已落地。当前单一运维人员、单一平台管理员的规模不设置专用状态审计表。生产仍需把 `/site-status.json` 接到前端版本目录之外并完成 API 停止、PWA、无痕窗口、移动端与日夜主题验收。
- 赛季管理员角色已进入开发基线但尚不可上线：共享 `user / season_admin / admin` 与显式 permission 矩阵、特权请求数据库当前角色复核、独立用户分页/角色变更接口、refresh token 撤销、最后一个平台管理员并发保护、赛季管理审计表骨架和停机迁移已落地；排位、主题赛季和入口显示已声明窄权限，赛季管理员同时复用管理员历史页查看全部历史记录及双方玩家视角只读回放，可按排位赛季或娱乐模式活动筛选，但不能导出原始回放包；排位公告入口已显式标注，娱乐模式公告可与玩家说明分别编辑。前端按权限投影赛季运营中心及平台管理员用户管理页。角色可在账号列表中直接选择并保存，不要求原因或持久审计。赛季写操作的持久审计、破坏性操作原因输入、完整负向矩阵与 E2E 尚未完成前，不得向真实运营账号分配 `season_admin`。
- 管理员能力已收敛到统一“运营管理中心”，按内容与平台、卡牌与规则、对局与赛季组织并复用既有模块页面；主页不再平铺六个管理入口。管理中心可分别控制赛季排位与主题赛季是否出现在玩家大厅和对局准备页，入口显示与各赛季生命周期、开放时段和候场运行保持独立。平台管理员的数据维护页可预览并显式确认清理 10 天前已封存回放，在排位长期卡组观察缺失时阻断，并按下拉框选定赛季，以只读一致性事务生成匿名化原始分析 ZIP：包含排位结果、积分流水/步骤、种子/投影和长期主卡组观察，不读取或导出 checkpoint、完整对局记录、timeline、聊天与回放；自动低峰调度和持久化操作审计仍未实现。卡牌效果 AI 提取已改为管理员专用服务端链路：PostgreSQL 保存 revision 配置和 AES-256-GCM Key 密文，部署环境控制主密钥、上游白名单与资源硬上限，候选测试不保存，浏览器只提交卡号并接收待确认文本，旧 DashScope 开发代理已移除。
- 候场等待音乐已改为服务端公共曲库：原有三首内置曲目通过 `0038_add_matchmaking_bgm_library.sql` 登记，平台管理员可在运营管理中心上传、试听和删除 MP3，并维护默认播放子集；用户可在声音设置中跟随平台默认或保存自己的曲目子集，显式空子集表示候场静音。上传资源使用公开对象存储的内容寻址路径，候场只从当前有效子集中随机播放，空库、无有效曲目或音频读取失败不影响匹配流程。匹配成功提示音仍是独立固定资源。
- 未登录根路径已从纯登录表单调整为公开产品首页，以真实卡牌和共享对战桌预览建立 Loveca 视觉主题；卡组、对局与观战入口会进入对应任务，需要登录的操作在认证后继续原目标，离线模式直接进入对局准备。首页同时承担非官方中文资讯入口，当前展示带 Bushiroad 来源、发售日期和日本语版标识的近期官方商品。认证外壳、登录后玩家大厅和对局准备页，以及卡组管理/编辑/共享、公共牌桌/房间/排位/主题赛季/观战/历史、账户和管理后台，均已迁移到共享 `ProductHeader` / `PageHeader` 与 workbench/list/form/status 组件语言；顶部导航、公告入口、主题语义色和返回行为已统一，对局准备按快速匹配、赛季活动和其他对战组织当前可见入口。共享卡组和公开观战入口使用同一品牌顶栏；沉浸式对局继续按规范不显示全站主导航。系统字体、400/600/700 字重角色、普通页密度、10px 面板和 8px 控件形状已形成共享 token，并提供 typed 的 Panel、SectionHeading、ActionButton、StatusBadge 与 TextInput 边界；固定主题/视口的 Playwright 截图差异测试覆盖首批代表页面。公开首页和子页面已经按中文文案规范统一“卡组 / 构筑 / 对战 / 对局 / LIVE”等玩家用语，并明确 Love Live! Series Official Card Game 非官方工具身份。
- 本地调试、对墙打、正式联机、远程调试、观战和历史回放继续复用共享 `GameBoard` / `PlayerArea`。
- 公共牌桌、房间联机、对墙打和调试入口复用卡图驱动的合法卡组选择网格与最近使用偏好；主页对局入口仅保留名称与模式标签，公共牌桌收敛为单一“找对手”主操作，候场/确认状态在可用页面区域居中，玩家文案不展示发布批次、状态机或具体分享渠道。
- 正式联机准备页已区分房间外与房间内场景：未进入房间时使用紧凑房间操作栏，不再展示空席位和未来流程；进入后以双席房间控制器集中呈现房间号、双方锁组/准备状态和唯一下一步操作，移动端主操作固定在底部。普通房间准备阶段可“退出房间”；开局猜拳页按沉浸式桌面隐藏返回与退出操作，公共牌桌配对会按到场或开局超时清理。未结束对局“返回大厅”保留房间恢复入口；进行中只保留“返回大厅 / 认输”。私有 `ONLINE_ROOM` 与公共牌桌赛后直接提供双方同意的“再来一局”，返回大厅继续保留房间入口，另以确认式“离开房间”释放占用；赛季排位赛后返回大厅仍释放旧房间占用。公共牌桌会刷新当前票据，并为待确认、创建中和已匹配状态提供明确操作，同一配对只自动引导一次。
- 卡组管理的行级菜单入口已由低识别度的三点图标改为与“编辑”同层级的“更多”文字按钮，复制、分享和删除等菜单行为保持不变。
- 桌面和移动端已完成主要布局、能量牌架、撤销入口、休息室统计、弹层层级、reduced-motion 和异步竞态收口；本地调试与对墙打共用确认式“重开对局”入口，服务端对墙打会封存旧局并沿用锁定卡组快照创建新局。
- 双方轻量弃牌展示已进入前端基线：正式联机、对墙打和本地调试统一消费公开 `HAND -> WAITING_ROOM` 事件及 `movementBatchId`，按批固定展示 1180ms，不阻塞规则、命令或下一效果；连续批次串行播放并在入队时遮挡目标实体，自客户端首次接收入队起超过 5 秒则解除遮挡并丢弃，TTL 使用客户端单调时钟。公共日志和弃牌播放泵更新不会使已排程的普通飞牌失效。教程保留单视角差分 fallback，观战与历史回放不播放。队列已覆盖增量补拉、去重、迟到丢弃、重连和撤销回退，桌面／390×844 布局及 800ms reduced-motion 静态展牌已有自动测试。
- 新手教程已建立四章连续教学基线：未登录访客和登录玩家通过服务端临时 `GameSession`、确定性随机决策带与私密脚本对手完成真实规则流程，客户端只消费 `PlayerViewState`、公开步骤和可见对象绑定，并继续复用共享 `GameBoard` / `PlayerArea`。教程提供章节检查点、退出、重开、响应式聚焦与 reduced-motion 支持，不写正式对局记录；当前仍需完成人工多视口、键盘和隐藏信息体验验收。详细产品与实现边界见 `docs/tutorial/`。
- 判定面板显式订阅桌面区域与卡牌投影；对墙打或联机中放置、翻开 LIVE 卡后，LIVE 需求预览不再沿用开局时的空区域缓存。
- 卡牌数据已将新推出的官方 BLADE Heart 颜色 `ORANGE` 作为独立指定色，并区分 `GRAY` 无色 Heart 与 `RAINBOW` All Heart；`double` 展开为两个独立 `GRAY` 判心项。当前只保守补齐数据、判定与显示能力，不得因新增颜色而自动扩大旧卡文本明确列出的六色范围。
- 新云端卡组默认包含 12 张 `LL-E-001-SD` 能量卡，并支持复制为新版本、分享管理与 DeckLog/YAML 导入。离线 guest 也可使用同一套编辑器从空白、YAML 或内置示例创建卡组；本地卡组按版本化结构保存在当前浏览器，可直接用于本地对墙打和双人调试，并明确提示用 YAML 备份。内置示例包含入门卡组及 2026-08-12 固化的 DeckLog `1Y9J3S`、`222H9S`、`1YWYS4`、`N33A0` 构筑。
- 已登录用户可从首页进入个人中心，修改用户名、显示名称和密码；邮箱换绑采用新邮箱确认后生效并撤销旧会话的流程。
- 玩家自定义游戏桌壁纸首版纵向链路已进入实现基线：个人中心提供受控纯色、宽屏/紧凑双布局预览、独立日间/夜间遮罩预览、图片焦点调整、统一保存和恢复默认；所有共享 `GameBoard` 复用 `BoardBackground`，壁纸只作为当前查看账号的私有展示偏好，不进入权威对局状态、历史或回放。服务端使用独立私有 MinIO bucket、Sharp 规范化、PostgreSQL 版本/北京时间每日额度/持久幂等和管理员移除审计；生产上线前仍需完成私有 bucket、容量、内容运营及完整环境视觉/E2E 确认。
- 排位与娱乐模式共用的活动大封面已进入首批开发基线：单母图生成宽屏 `16:7` 与紧凑 `4:3` WebP，当前配置通过 `0034_add_activity_covers.sql` 保存 revision、最后幂等、双构图和公开对象引用；图片处理与上传限频从玩家壁纸链路抽取为无业务归属的共享底座，公共资源使用现有 bucket 的 `activity-covers/` 独立命名空间。排位／娱乐模式主投影直接携带当前封面，共用 Hero 只请求当前响应式槽位并保持默认视觉降级；两类管理页复用双预览、焦点／缩放、受控遮罩、替换和带原因移除，成功配置切换与管理审计同事务。当前仅为开发工作树基线，尚未执行生产迁移，也未完成孤立对象定期扫描、真实对象存储集成、代表素材视觉／E2E、LCP／体积观测和版权运营验收。
- 当前候选版本以根目录 `VERSION` 为准。发布、镜像、数据库迁移和卡牌同步仍按 release skill、runbook 与 migration notes 执行，不能从本文件的旧窗口描述推断生产状态。

## 当前事实来源

| 主题               | 权威来源                                                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------- |
| 项目范围与产品能力 | `docs/PROJECT_REQUIREMENTS.md`、`docs/system-design.md`                                                           |
| 对战模式与只读边界 | `docs/battle-mode-purpose-and-boundaries.md`                                                                      |
| 联机现状与限制     | `docs/online-mode/preparation.md`、`docs/online-mode/room-exit-and-recovery.md`                                                  |
| 对局记录与回放     | `docs/match-replay/requirements.md`、`docs/match-replay/design.md`、`docs/match-replay/serialization-contract.md` |
| 卡效完成状态       | `docs/card-effect-reuse-audit/existing_module_map.md`                                                             |
| 卡效开发规范       | `AGENTS.md`、`docs/card-effect-framework/`、`docs/card-effect-reuse-audit/`                                       |
| 版本与发布         | `VERSION`、package 版本、release runbook、`drizzle/migration-notes/`                                              |
| 历史施工过程       | Git 提交历史                                                                                                      |

## 仍有效的主要缺口

1. 全卡池完整自动裁判、完整 trigger matcher 接线和更广泛的事件语义仍需按真实卡效分批推进。
2. 正式联机运行态持久恢复、完整随机记录、完整 decision record、自由拖拽/手动处理原因结构化和确定性重演尚未闭环。
3. AI 对战当前支持原始 μ's 预组、绿莲-6弹ver与 Like a Treasure 的真人/AI 镜像和交叉选择，并提供 AI 专用蓝紫构筑；前两副已登记构筑的规则窗口与确定性策略通局已有覆盖，蓝紫已完成目录、冻结加载与策略手册接入，并补齐部分卡效窗口的 AI 查询与正常命令回归（范围见支持矩阵），尚未完成完整确定性通局或真实模型验收。Like a Treasure 按用户附件固定 48/12 构筑与 v0.2 手册，12 普通能量；已补确认式特殊登场、休息室起动、单槽选择和所遇卡效查询，并完成两局确定性镜像自然终局；本机发布卡库/PT 只读校验为 9/9、前端双席选择与手册联动已通过，已有该构筑的本地真人/模型试玩归档，尚未形成策略强度验收结论。真实模型的连续回合规划、高分／多 LIVE 准备及选择说明准确性仍未通过验收。已加入共享能力费用/目标查询、接受动作后的有界回合上下文、合法已知顶牌记忆与输入无损去重；固定局面的原槽登场接回能/回收已有完整命令回归。LIVE 设置已补齐实际盖牌额度、重复手牌与印刷声援心数基线，策略按表演目标、近期保留集合和周转顺序核对；提前确认、未来预算说明及盖牌计划执行仍需验收，固定局面复现方法见[完整环境验证](docs/ai-battle/full-environment-validation.md)。当前仍需验收多 LIVE 需求合计、完整费用路线与选择说明准确性；局部改善不能代表整局策略质量。复测见[空费与回合规划](docs/ai-battle/reviews/2026-09-10-f978-planning.md)。历史证据见[手牌规划复测](docs/ai-battle/reviews/2026-09-09-hand-planning.md)、[绿莲费用衔接](docs/ai-battle/reviews/2026-09-09-c530c1a3-28.md)、[绿莲补场取舍](docs/ai-battle/reviews/2026-09-09-3a5522fa-73.md)和[绿莲资源顺序](docs/ai-battle/reviews/2026-09-10-3d6b3a8c-58-69.md)。其他构筑的起动、盲选、数字及站位窗口需按真实需求继续适配；完整日志覆盖、生产迁移与开放仍需验收。开发支持范围和真实结果分别由 [AI 支持矩阵](docs/ai-battle/support-matrix.md) 与 [真实模型验证](docs/ai-battle/model-validation.md)维护。
4. 轮换主题牌桌还缺首期冻结预组与真实实测组合、主题身份历史展示、完整等待/确认/组合胜率与异常率指标、选用分布告警、跨进程恢复和双浏览器验收；开局后的断线裁定应复用排位现有的 1 分钟重连期限与在线代际保护，单方超时由超时方判负并写入主题赛季胜负统计，但不引入积分、段位或排行榜；当前实现不得直接作为生产活动开放。
5. 非对局页面已进入统一视觉系统，首批 macOS 固定主题/视口截图基线已建立；仍需在统一 Chromium 与 CJK 字体环境补 Linux CI 基线、扩大代表页面覆盖，并在不改变规则桌内部布局的前提下推进对局外层 UI。
6. 前端仍有大 chunk 告警，后续需要继续拆分由全局 store 拉入的 battle runtime。
7. 发布、镜像推送、生产迁移、卡牌数据正式同步和对象存储写入均是独立高风险动作，必须按对应流程取得授权。
8. 赛季管理员上线前仍需把排位、主题赛季和入口显示的全部写操作接入持久化审计，给排位草稿删除与主题预组退休补非空原因，并完成权限隔离、即时失权、用户管理和最后管理员保护的完整集成/E2E 验收。
9. 活动大封面上线前仍需接入超过 24 小时的 `activity-covers/` 孤立对象扫描与失败重试，完成真实 MinIO／PostgreSQL 原子发布和迁移演练、断点两侧日夜主题截图、双浏览器管理／玩家流程、LCP／资源体积与版权移除流程验收；当前 `season_admin` 同时拥有排位与娱乐模式权限，若要分别委派给不同账号需另行扩展授权模型。

### 已确认待修事项

- 旧精确卡号卡效定义需按基础编号收束，并同步核对 workflow、continuous 查询和罕度测试；实际覆盖见 `docs/card-effect-reuse-audit/existing_module_map.md`。
- 普通 `PLAY_MEMBER_TO_SLOT` 仍按前 N 张活跃能量支付，尚未接入卡效内部已有的特殊能量选择；修复应收口到公共支付边界，不能由 UI 选牌绕过权威命令。当前路径见 `src/application/game-session.ts`。
- 旧密码包装格式仍在成功登录后升级，与禁用运行时懒迁移的原则存在偏差；需先明确凭据停机转换与账号恢复方案，再收束读取路径。当前行为见 `docs/system-design.md` 的认证与会话说明。
- 卡组读取在类型缺失且 resolver 无结果时仍按前缀猜类型；需结合存量数据停机转换移除该 fallback。实际读取与服务端保存的区别见 `docs/deck-management/design.md`。

## 下一步优先级

1. 赛季排位下一步按发布 Runbook 完成卡组分类、玩家查询与两块环境展示增量的生产迁移/种子/首次发布验收，再用已有对局数据回放验证 V4 的排行榜分布、单局极值和玩家时段净变化，并在现有运营概览基础上补齐外部告警与跨日趋势。公共牌桌继续收束配对确认超时、房间引导失败恢复、开局失联恢复和聚合指标读取；整站维护按发布 Runbook 完成生产快照接线及停机/恢复矩阵验证；轮换主题牌桌先完成真实预组试打矩阵、指标/告警和双浏览器验收，再讨论生产活动开放。
2. 赛季管理员继续完成赛季写操作持久审计、破坏性操作原因和完整权限/E2E 验收；在此之前保持不可生产委派状态。
3. 卡效开发继续以主登记册选择能推进真实事件边界、when-if、selector、公开/检视 workflow 或 LIVE modifier 的样例；每张卡实时更新登记册和 focused tests。
4. 继续缩小 runner 胶水和重复 workflow，但只在出现第二个真实样例时晋升 shared family，不建立任意步骤解释器。
5. 继续完善 LIVE 自动判定、效果顺序、撤销、每回合限制和跨回合事件边界测试。
6. 回放方向只维护当前需求、设计和序列化契约；已完成阶段不再新增实施流水账。
7. UI 统一下一步只收束对局外层导航、状态与品牌外壳，不改变 `GameBoard` / `PlayerArea` 的规则布局；同时持续维护非对局页面的多主题、多视口截图基线。
8. 活动大封面下一批先实现 `activity-covers/` 超过 24 小时孤立对象扫描与可重试清理，再补真实对象存储／数据库集成和管理端、玩家端的断点视觉／E2E 验收；未完成这些边界前不执行生产迁移。
9. 新手教程下一步由人工完成 `basic-live-loop@1.1.6` 四个章节入口及单局三回合步骤流的真实通关验收。重点覆盖章节检查点状态、两步换牌确认与动态聚焦、上一步只读回看、对手行动可读停留、成员登场的拖放/点选双路径、三轮判定与成功 LIVE 处理、所有 A→B 动作的双高亮与说明框零重叠、费用 9「钟岚珠」的手牌聚焦和自动支付、抽弃效果面板、分数 1「Very! Very! COCO夏っ」的 LIVE 开始能力、费用 2「绚濑绘里」自送休息室后公开回收并用剩余 4 点能量让费用 4「桂城 泉」再次登场、第二与第三回合对手里侧 LIVE 盖牌停留及双方依次表演、对手场攻按 3 → 7 → 12 连续增长、第二回合指定色不足失败、对手最终分数 2 LIVE 成功，以及第四章 `5 Heart + 4 判 = 9 场攻`、两张 LIVE 合计需求与四张 Heart 判心的一致性。保持教程引导层独立，不在普通对局或共享组件中散落 `isTutorial` 规则分支。
